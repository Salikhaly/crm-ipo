# Аудит UX v3 (13.07.2026, агент-дизайнер) — что осталось внедрить

Сделано в PR#16: тост не обрезается (#12), чип фильтра над канбаном (#6), «Сделать текущим» скрыт на активном (#21), autoFocus без прыжка клавиатуры (#23), title у кнопки обновления (#16), бейдж задач = горящие, лимит файлов 20МБ единый, undo этапа из карточки.

## КРИТ — мобильный сценарий — ✅ СДЕЛАНО 13.07 (все 4 + тач-цели #10 частично: быстрые действия канбана 36px)
1. `globals.css:248` + `features/wa:855` — панель клиента в WA-чате скрыта на ≤768px → «Открыть карточку» и «Рассчитать» недоступны с телефона; иконка ti-user в шапке чата ничего не делает. Фикс: bottom-sheet.
2. `features/clients:858-916` + `pages/index:2864-2869` — инлайновые gridTemplateColumns '1fr 1fr 1fr' не ловятся медиа-правилом → на телефоне поля сжаты в 3 колонки. Фикс: классы .r3/.r2 из globals.css:227.
3. `pages/index:1856` — HTML5 draggable не работает на таче → перенос по канбану с телефона невозможен. Фикс: кнопка «⋮ → Переместить на этап…» на карточке, зовёт onMove.
4. `pages/index:287,1592` — «Список клиентов» недостижим с телефона (нет в bottom-nav и PAGE_ORDER), KPI только скрытым свайпом. Фикс: пункт «Ещё» в нижней навигации.

## ВЫС (5,7,8,22 + кружки/шрифты — ✅ сделано 13.07; ✅ ВСЁ сделано (9,11 в PR#16))
5. «Не трогали 3+ дней» читает cm.createdAt, а комментарии пишут поле date → блок врёт. Фикс: писать createdAt ISO в комменты + fallback updatedAt (pages/index:1643).
7. Слияние дублей/масс.действия мертвы в виде «Карточки». Фикс: авто-баннер «Найдено N дублей → Объединить».
8. Тур: 4 абстрактных шага, ключ crm_tour_done на браузер (не user), нельзя перезапустить. Фикс: ключ с user.id, пункт «? Обучение» в сайдбаре, +шаги про Список/виды/дубли/свайп/«/».
9. features/wa:23-76 — ТРИ механизма шаблонов (MSG_TEMPLATES хардкод, «Быстрые ответы» хардкод, «/» из админки). Оставить один — админский.
10. Тач-цели: быстрые действия канбана padding 5px→9px+minHeight 38; кружки задач 18-20px→24px+padding.
11. MoneyInp с разделителями тысяч для сумм договора/дохода (ui.jsx:51, clients:903,1014).

## СРЕД/НИЗК (13 ✅ сделано; осталось 14,15,17,18,19,20,25 — мелкие)
13. Калькулятор: «НОВЫЙ КАЛЬКУЛЯТОР»/«РАСЧЁТЫ ЧЕРЕЗ API» → «Расчёты для клиента»/«Служебные»; вкладки 10px → 12px (calc:109-128).
14. .help-tip только :hover → не работает на таче (globals.css:351).
15. Убрать дубли Импорт/Экспорт из топбара (остались подписанные на странице).
17. Точки свайпа 6px кликабельные — сделать индикатором.
18. window.prompt для сохранения вида → инлайн-поле; виды в users.settings вместо localStorage.
19. «+ Задача»: select клиентов без поиска/сортировки → комбо с фильтрацией.
20. Ctrl+K: стрелки по результатам; на мобильном две одинаковые лупы.
22. textarea WA rows=1 без авто-роста → расчёт из 8 строк не проверить.
24. Мелкий текст 9-9.5px поднять до 10.5-11px (роль в сайдбаре, «без задачи»).
25. Разнобой кнопок/чипов (радиусы 4/5/6/20) → расширить components/ui.jsx, заменить самописные.
